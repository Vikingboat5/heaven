# 种子与物品体系 Spec（旅行内容化重构）

- 版本：v0.1（草案，待拍板后转正式）
- 日期：2026-08-17
- 地位：MVP Spec v1.1 之后的功能扩展规格，落地后并入 `mvp-spec.md` 升 v1.2
- 关联决策：ADR-001（极简重构）；本文档定稿时同步产出 ADR-002
- **验收约定（负责人要求，2026-08-17 起生效）：本文档中每个用户操作/页面都给出明确的预期结果，开发实现与测试验收以此为唯一准绳，不允许出现"实现了一个版本、验收是另一个版本"的偏差**

---

## 0. 设计红线（已拍板，不可违反）

| # | 红线 | 含义 |
|---|------|------|
| R1 | **纯牵挂，零损失厌恶** | 没有任何"不回来就亏/被拿走"的机制；"失去"只以消耗/交换/赠予的叙事形式存在 |
| R2 | **交换永不降级** | 交换所得品级 ≥ 带出品级；系统不得在用户离线时替用户做亏本决定 |
| R3 | **传说品级不可消耗** | 传说品不出现在口粮属性上；收藏品永不丢失 |
| R4 | **不允许加速** | 旅行时长不可付费/操作缩短 |
| R5 | **陌生化转述** | 种子角色/地点在日记中以宠物视角描述，不出现真实人物/作品原名（沿用 Q6 IP 转译方法论） |

## 1. 目标用户与心理模型

面向 **抽卡 + 集邮 + 养成 + 轻游戏** 群体（参照：旅行青蛙 / QQ农场 / 月兔旅行）：

- 爽点核心 = **归来开卡**（每日 1-2 次迷你抽卡：品级光效 + NEW! 标记 + 图鉴进度）
- 留存核心 = **图鉴完成度长尾** + **种子包持续上新**（每波新包 = 一次回归理由）
- 情绪核心 = 牵挂（等待）+ 认出（种子共鸣）+ 被记住（对话/首发现冠名）

## 2. 术语

| 术语 | 定义 |
|------|------|
| 种子(Seed) | 一次旅行的叙事单元：地点背景 + 角色卡 + loot 表 + 事件卡组 + 氛围词，打包定义 |
| 种子包(Pack) | 种子的分组（图鉴分组/上新运营单元），如"山海经·东" |
| 基调(Flavor) | 一趟旅行的主旋律，先抽基调再围绕它抽事件 |
| ItemDef | 物品静态定义（内容文件）；发现状态（首发现者/时间）在 DB |
| 行囊(Loadout) | 出门前打包的 3 槽位：口粮×1 + 伴手礼×1 + 护身符×1 |

## 3. 种子模型

```yaml
# 单个种子（内容文件，版本化，随代码提交）
seed:
  id: myth:moon_palace          # 命名空间: 线:id
  pack: myth_shanhaijing_east   # 所属包（图鉴分组）
  name: 月宫                    # 内部名（不直接外露给玩家, 玩家看到的是陌生化描述）
  min_level: 1
  time_gate: null               # 或 {"festival": "mid_autumn", "window_days": 3}
  background: |                 # 给 LLM 的背景卡（成文时注入）
    终年桂花香的月面宫殿, 白玉台阶, 云雾缭绕…
  cast:                         # 角色卡
    - id: npc:jade_rabbit
      desc_words: 捣药的白兔, 红眼睛, 围裙    # 陌生化锚点: 视觉+感官, 不给真名
      interactions: [receive_gift, exchange]
    - id: npc:wugang
      desc_words: 砍一棵永远砍不倒的树的高大男人
      interactions: [meet_character]
  loot_table:                   # 物品产出表: item_id → 权重(品级隐含在 ItemDef)
    - {item: osmanthus_cake, weight: 40}
    - {item: moonstone, weight: 15}
    - {item: moon_herb, weight: 8}
  event_deck:                   # 事件卡组, 按基调分类
    encounter: [...]            # 际遇回可用
    food: [...]                 # 美食回可用
    scenery: [...]              # 闲逛回/留白回通用
  atmosphere: 桂花香/银白色/安静   # 氛围词(注入 LLM prompt)
```

**校验测试（内容进库的硬门槛）**：结构合法 / 引用的 item、npc id 全部存在 / 事件卡组基调标签合法 / 全文本过敏感词 / loot 权重和=100。

## 4. 种子宇宙全景与上线波次

六大主线，设计全景、分波上线。**图鉴按包分组显示完成度**（如"西游图鉴 7/10"）。

### 线一·上古神话与山海经
创世神话（昆仑墟/不周山/女娲补天台/扶桑汤谷/建木）、山海经·东（青丘/丹穴山凤凰/招摇山）、山海经·西（钟山烛龙/长留山/归墟/轩辕丘）、道教仙山（蓬莱/方丈/瀛洲/武当/青城）、佛教圣境（灵山/普陀山/五台山/峨眉山/九华山）、民间神仙（月老祠/财神庙/妈祖湄洲/济公灵隐寺）

### 线二·名著宇宙
西游记（花果山/火焰山/五庄观/高老庄/流沙河/通天河/女儿国/盘丝洞/东海龙宫/蟠桃园）、封神演义（陈塘关/朝歌/西岐/玉虚宫）、三国演义（赤壁/隆中/长坂坡/白帝城/五丈原/桃园）、水浒传（梁山泊/景阳冈/野猪林）、红楼梦（大观园/太虚幻境）、聊斋志异（兰若寺/狐仙洞）、镜花缘（君子国/女儿国/两面国）

### 线三·历史现场
古都（长安西市/洛阳龙门/汴梁虹桥/临安御街/金陵秦淮河/殷墟）、丝路（敦煌莫高窟/玉门关/阳关/楼兰/喀什巴扎）、战场（赤壁/官渡/垓下/淝水/雁门关）、文人地图（庐山瀑布/黄鹤楼/岳阳楼/滕王阁/寒山寺/敬亭山/杜甫草堂/桃花源）、工程奇迹（都江堰/赵州桥/大运河/长城）、神秘考古（三星堆/兵马俑/马王堆）

### 线四·华夏大地
三山五岳、江湖水色（西湖/洱海/天池/纳木错/九寨沟/三峡/壶口/钱塘潮）、地质奇观（张家界/张掖丹霞/桂林/鸣沙山月牙泉/元阳梯田/茶卡盐湖）、城市烟火（成都茶馆/重庆洪崖洞/苏州园林/广州早茶/西安回民街/北京胡同/哈尔滨冰灯/拉萨八廓街）、节令民俗（春节庙会/元宵灯会/端午龙舟/七夕/中秋/重阳/泼水节/火把节/那达慕——全部时间门控）

### 线五·世界文明
文明古国（金字塔/雅典卫城/斗兽场/马丘比丘/奇琴伊察/吴哥窟/泰姬陵/佩特拉）、欧洲（巴黎/威尼斯/圣托里尼/冰岛极光/挪威峡湾/荷兰风车）、亚太（京都岚山/富士山/北海道/曼谷/巴厘岛）、美洲（大峡谷/黄石/亡灵节门控/亚马逊）、非洲大洋洲（肯尼亚迁徙/撒哈拉/大堡礁/萤火虫洞）、极地（北极/南极——最高等级解锁）

### 线六·幻想转译（远期，T2 灰色，待首批验证后评估）

### 上线波次

| 波次 | 内容 | 种子数 | 目的 |
|------|------|--------|------|
| W1 打样 | 月宫 | 1 | 全链路验证（内容→引擎→日记→开卡→图鉴） |
| W2 首发 | 山海经·东、西游记、古都、城市烟火、文明古国 | ~35 | 认知覆盖率最大化 |
| W3-W5 | 三国/封神/文人地图/江湖水色/欧洲… | 每 2-3 周一包 8-10 | 持续运营，每包=回归理由 |
| 长尾 | 节令包随真实节气；聊斋/镜花缘/世界神话 | 持续 | 年度内容管线 |

**等级分层**：线四/线一低门槛（Lv1-3），线三中（Lv3-5），线五高（Lv5+），极地顶级。

## 5. 物品体系

### 5.1 ItemDef 字段

```yaml
item:
  id: osmanthus_cake
  name: 桂花糕                 # 玩家可见名(物品名可直接显示, 地点/角色名才需陌生化)
  desc: 带着月宫桂花香的糕点
  rarity: common               # common(寻常) / rare(稀罕) / epic(传世) ← 品级文案待拍板
  attr: food                   # food(口粮) / gift(伴手礼) / charm(护身符) / antique(古董)
  appearance: 菱形白糕, 缀金色桂花   # 生图用外观描述词
  image: /static/items/osmanthus_cake.png
  source_seed: myth:moon_palace
```

### 5.2 品级 × 属性矩阵

| | 口粮 food | 伴手礼 gift | 护身符 charm | 古董 antique |
|---|---|---|---|---|
| 寻常 common | ✅ 主力 | ✅ 主力 | ✅ | ✅ |
| 稀罕 rare | ✅ 封顶 | ✅ | ✅ | ✅ |
| 传世 epic | ❌（R3） | ✅ | ✅ | ✅ |

### 5.3 物品图片（预生成，不走运行时）

- 内容进库时批量生成：单帧道具图标，统一软萌插画风，**不跟随宠物画风**（已拍板：图鉴是社交货币，同物同图）
- prompt 模板："单个游戏道具图标，{appearance}，居中，纯白背景无阴影"；复用 petgen 的白底抠图/QC/落盘
- 落盘 `backend/static/items/{item_id}.png`，随代码提交；生成失败 → QC 拦截在入库前，运行时零失败

### 5.4 存储两层

| 层 | 内容 | 位置 |
|---|---|---|
| ItemDef 静态定义 | 见 5.1 | 内容文件（配置即代码，版本化+单测） |
| 发现状态 | first_discovered_by(user_id) / first_discovered_at | DB 表，启动时从内容文件幂等播种，运行时仅回填一次 |
| Inventory | item_id 引用 + count + acquired_at + acquired_zone + acquired_via(forage/exchange/gift) | pet.inventory JSON 升级 |

**存量迁移**：旧 inventory 为名字字符串 → 一次性映射名字→item_id，映射失败归入"失落的纪念品"兜底条目。

## 6. 行囊系统

- **背包（收藏库）无上限**；**行囊 3 槽位**：口粮×1 + 伴手礼×1 + 护身符×1，每格可空
- 空手出门合法，不惩罚；打包出门产出更好（正向表达，非负向惩罚）
- 消耗品产出/消耗比 ≥ 1.5:1（数值表定档）

## 7. 旅行引擎

### 7.1 一趟旅行的流程（确定性边界：收获/品级/消耗规则引擎定，措辞 LLM 定）

```
选种子(Q6偏好加权+等级门控+时间门控) → 抽基调(性格加权) → 从种子事件卡组按基调抽事件
→ 结算(loot品级抽取/口粮消耗/交换/赠予) → LLM 带种子背景成文(1次lite, 降级模板) → AdventureLog
```

### 7.2 基调表（10 种，性格加权）

| 基调 | 内容 | 基准权重 | 性格加成 |
|------|------|---------|---------|
| 际遇回 | 遇到种子角色 | 12% | 外向↑ |
| 交友回 | 遇到动物伙伴 | 12% | 外向↑、胆小↓ |
| 收集回 | loot 探索收获 | 18% | 开放↑ |
| 美食回 | 品尝当地吃食 | 10% | 吃货↑↑ |
| 学艺回 | 观摩学到小把戏 | 8% | 尽责↑ |
| 助人回 | 帮了谁小忙 | 10% | 亲和↑ |
| 惊险回 | 虚惊一场，**安全解决零损失**（R1） | 8% | 稳定高↑、胆小↓ |
| 寻宝回 | 高品级产出 | 7% | 寻宝鼻↑ |
| 闲逛回 | 风物体验 | 10% | — |
| 留白回 | 什么都没干睡一觉回来，**保底小物** | 5% | 稳定高↑ |

### 7.3 事件类型表（14 种，结算规则）

| 类型 | 物品变化 | 说明 |
|------|---------|------|
| meet_character | 可能获赠 | 陌生化转述（R5） |
| exchange | 置换，**不降级**（R2） | 需行囊有伴手礼 |
| receive_gift | 获得 | 际遇/助人常见 |
| taste | 可能获得口粮 | 美食回主体 |
| learn | 无（高经验） | |
| help | 常获谢礼 | |
| weather | 无 | |
| festival | 限定物品 | 时间门控 |
| danger_safe | **绝不损失**（R1） | 高经验 |
| rest_nothing | 保底小物 | 留白回主体 |
| find_item | 获得（loot 表） | |
| treasure | 获得（稀罕/传世） | |
| lost_way | 跨种子彩蛋 | 种子网络横向连接 |
| homesick | 无 | 情感节拍 |

## 8. 交换系统（L1 单机版）

- 交换对象 = 种子角色卡（`npc:*`），如"在火焰山用团扇换到芭蕉形叶子"
- 交换所得品级 ≥ 带出品级（R2）；换回什么未知（保留开卡感）
- 是否发起交换由宠物性格决定；不交换=原样带回（性格表达，文案兜底成萌点）
- provider 接口隔离：`resolve_exchange(gift, seed) -> (counterpart_ref, received_item)`，L1 从种子 cast 选；未来 L2 真人撮合时替换实现，调用方不变

## 9. 首发现（冠名）

- 粒度：物品定义级；字段 `first_discovered_by(user_id)` / `first_discovered_at`，发现即检查回填
- 纯荣誉徽章，**无实质加成**（防竞速焦虑，守 R1）
- 单用户期呈现为"你的团团于 X 首次发现"；多用户上线自动升级为全服冠名

## 10. 社交就绪接缝（只留缝，不建表）

1. 所有"引用角色"的字段 = 带类型复合引用：`npc:yutu` / `pet:42` / `user:7`，禁止裸 ID
2. "离线期间发生的事"统一为事件 feed；回归卡片是 feed 的一种（`travel_returned`），未来"来访/收礼"为新事件类型
3. 对外展示名一律经 `display_name()` 函数（现返回 username，未来读 display_name 字段）
4. 明确不建：好友表/互访表/挂单表；不开 `/api/social/*`

## 11. 页面与操作预期表（**验收准绳**：每个操作 → 明确的预期结果）

### 11.1 主页（Home）

| # | 用户操作 | 预期结果 |
|---|---------|---------|
| H1 | 打开 App，宠物在家 | 显示宠物序列帧动画、名字/等级/性格标签；出现「和TA聊聊」「打包行李」「旅行日记」入口 |
| H2 | 打开 App，宠物旅行中 | 显示空房+目的地名+回来倒计时；聊天入口置灰并提示"它出门旅行啦" |
| H3 | 打开 App，宠物刚归来 | 弹出回归卡片：旅行日记全文 + 获得物品（带品级光效，新物品带 NEW! 标记）+ 经验值 |
| H4 | 关闭回归卡片 | 回到 H1 状态；本次收获已入背包与图鉴 |

### 11.2 行囊打包页（新页面）

| # | 用户操作 | 预期结果 |
|---|---------|---------|
| B1 | 进入打包页 | 显示 3 槽位（口粮/伴手礼/护身符）+ 背包可用物品网格（按属性过滤可选） |
| B2 | 点选物品入槽 | 物品进对应槽位；属性不匹配的槽位不可放入 |
| B3 | 点「送它出门」 | 宠物进入旅行中状态，页面切到 H2；行囊物品锁定（不可再编辑） |
| B4 | 空手点「送它出门」 | 正常出门（不阻止、不警告） |
| B5 | 旅行中进入打包页 | 只读展示当前行囊，不可编辑 |

### 11.3 图鉴页（新页面）

| # | 用户操作 | 预期结果 |
|---|---------|---------|
| C1 | 进入图鉴 | 按种子包分组的格子矩阵；已获得=彩色图，未获得=剪影；每包显示进度 n/N |
| C2 | 点开已获得的物品 | 详情：图片/名称/品级/属性/描述/来源地/获得时间；若为全球首发现，显示发现者与时间（单用户期显示"你的团团"） |
| C3 | 本次归来获得新物品后进入图鉴 | 对应格子带 NEW! 角标，进入后消除 |

### 11.4 旅行日记页（现有 AdventureView 升级）

| # | 用户操作 | 预期结果 |
|---|---------|---------|
| D1 | 进入日记列表 | 按时间倒序展示：每次旅行的日记正文+目的地+收获缩略图 |
| D2 | 点开某次旅行 | 全文日记 + 事件时间线 + 收获明细（物品图+品级） |
| D3 | 查看含交换事件的日记 | 交换双方与物品明确展示（"用 X 换到了 Y"） |

### 11.5 历史/发现页（新页面或并入图鉴）

| # | 用户操作 | 预期结果 |
|---|---------|---------|
| E1 | 查看某物品发现史 | 显示首发现者与时间（单用户期=个人记录） |

## 12. 全局验收标准

- 单元测试：种子内容校验（第 3 节）、基调权重归一、品级×属性约束（R3）、交换不降级（R2）、首发现回填、行囊槽位规则
- E2E：探针账号走"打包→出门→强制到期→归来→开卡→图鉴"全链路断言
- 浏览器走查：11.x 表逐条过
- LLM 环节故障 → 模板降级，数据不丢（沿用现有红线）

## 13. 内容生产流水线

```
按写作规范批量起草(背景卡/角色卡/loot表/事件卡组)
→ 负责人审稿定稿 → 进内容文件 → 校验测试(结构/引用/敏感词) → 物品图批量预生成 → QC → 入库
```

## 14. 待拍板清单

| # | 问题 | 建议 |
|---|------|------|
| Q1 | 品级文案 | "寻常/稀罕/传世"（治愈调性）替代"常见/稀有/传说" |
| Q2 | W2 首发包构成 | 山海经·东/西游记/古都/城市烟火/文明古国 是否调整 |
| Q3 | 历史页独立 or 并入图鉴 | 建议并入图鉴详情页（少一个页面） |
| Q4 | 留白回保底小物 | 建议给（一片叶子级），守 R1 的最后一度 |
